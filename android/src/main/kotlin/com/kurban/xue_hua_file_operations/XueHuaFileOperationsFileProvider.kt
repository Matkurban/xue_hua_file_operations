package com.kurban.xue_hua_file_operations

import androidx.core.content.FileProvider

/** Dedicated FileProvider subclass so this plugin does not collide with host/other providers. */
class XueHuaFileOperationsFileProvider : FileProvider()
